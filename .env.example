# Environment
NODE_ENV=development
PORT=3000

# Database (Supabase PostgreSQL)
# Get this from your Supabase project settings:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Click "Settings" -> "Database"
# 4. Copy the "Connection string" from the "URI" section
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Example: postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://user:password@localhost:5432/habit_tracker

# Optional: Gmail API Configuration (for future reminder feature)
# Get API key from Google Cloud Console:
# 1. Create a project in Google Cloud Console
# 2. Enable Gmail API
# 3. Create a service account and download the JSON key
GMAIL_API_KEY=

# Optional: Brave Search API Configuration (for future AI integration)
# Get API key from https://api.search.brave.com/
# Used for habit suggestions and recommendations
BRAVE_API_KEY=
