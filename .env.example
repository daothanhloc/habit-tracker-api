# Environment
NODE_ENV=development
PORT=3000

# Database (Supabase PostgreSQL)
# Get this from your Supabase project settings:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Click "Settings" -> "Database"
# 4. For production/Vercel: Use "Connection Pooling" string (port 6543, transaction mode)
# 5. For development: Use regular "Connection string" (port 5432)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Example (production): postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# Example (development): postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://user:password@localhost:5432/habit_tracker

# JWT Secrets (REQUIRED)
# Generate secure random strings for production:
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-this-in-production

# Optional: Gmail API Configuration (for future reminder feature)
# Get API key from Google Cloud Console:
# 1. Create a project in Google Cloud Console
# 2. Enable Gmail API
# 3. Create a service account and download the JSON key
GMAIL_API_KEY=

# Optional: Brave Search API Configuration (for future AI integration)
# Get API key from https://api.search.brave.com/
# Used for habit suggestions and recommendations
BRAVE_API_KEY=
